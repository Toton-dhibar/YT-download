# Environment variables for Flask backend
# Copy to .env and adjust as needed

# Flask settings
FLASK_ENV=production
FLASK_DEBUG=0
PORT=5000

# If you want to restrict CORS, set a comma-separated list of origins
# CORS_ORIGINS=https://your-frontend.example.com,https://another.example.com